import { Component } from "react";

class BasketModule extends Component {
    constructor( props ) {
        super( props );
    }

    render( ) {
        return(
            <div>
                <h1>Basket Module.</h1>
            </div>
        );
    }
}

export default BasketModule;